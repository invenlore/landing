import { Box } from "@mui/material";

export default function Home() {
  return (
    <Box
      sx={{
        p: 4,
        bgcolor: "background.default",
        color: "text.primary",
      }}
    >
      <span>psdfp</span>
    </Box>
  );
}
